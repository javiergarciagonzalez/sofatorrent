// const { fetchRarbgSearchResults } = require('../../search/rarbgApi');

describe('Rarbg API controller tests', () => {
    test('Returns results when searching for batman', () => {
        // expect(fetchRarbgSearchResults);
        // TODO
        expect(1).toBe(1);
    });
});
